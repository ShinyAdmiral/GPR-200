// GLSL STARTER CODE BY DANIEL S. BUCKSTEIN
//  -> IMAGE TAB (final)

//------------------------------------------------------------
// SHADERTOY MAIN

// mainImage: process the current pixel (exactly one call per pixel)
//    fragColor: output final color for current pixel
//    fragCoord: input location of current pixel in image (in pixels)
void mainImage(out color4 fragColor, in sCoord fragCoord)
{
    //get normalized screen coords
    sCoord uv = fragCoord / iChannelResolution[0].xy;
    
    //add convolution algorithm 
    //fragColor = add(iChannel0, iChannel1, uv);
    
    //screen convolution algorithm 
    //fragColor = screen(iChannel0, iChannel1, uv);
    
    //multiply convolution algorithm 
    //fragColor = multiply(iChannel0, iChannel1, uv);
    
    //get colors for next few algorithms
    vec4 color1 = texture(iChannel0, uv);
    vec4 color2 = texture(iChannel1, uv);
    
    //mix convolution algorithm 
    //fragColor = mix(color1, color2, sin(iTime) * 0.5f + 0.5f);
    
    //only for edge detection
    fragColor = color1;
}